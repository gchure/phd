language: ruby
cache: bundler
branches:
  only:
  - master
script:
  - JEKYLL_ENV= sh copyfigs.sh && production bundle exec jekyll build --destination site
deploy:
  provider: pages
  local-dir: ./site
  target-branch: release
  email: deploy@travis-ci.org
  name: Deployment Bot
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: true
  on:
    branch: master
